machine:
  services:
    - docker

dependencies:
  pre:
    - sudo add-apt-repository ppa:duggan/bats --yes
    - sudo apt-get update -qq
    - sudo apt-get install -qq bats

test:
  post:
    - make itest